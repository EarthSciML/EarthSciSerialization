{
  "id": "sibling_field",
  "description": "Sibling-field pattern variables (§5.2.4). Pattern D($u, wrt=$x) rewrites to index($u, $x); the $x in the wrt field is a name-class pattern variable that binds the string 'wrt' argument.",
  "rules": [
    {
      "name": "deriv_to_index",
      "pattern":     {"op": "D", "args": ["$u"], "wrt": "$x"},
      "replacement": {"op": "index", "args": ["$u", "$x"]}
    }
  ],
  "input": {"op": "D", "args": ["T"], "wrt": "t"},
  "expect": {
    "kind": "output",
    "canonical_json": "{\"args\":[\"T\",\"t\"],\"op\":\"index\"}"
  },
  "tags": ["sibling_field"]
}
