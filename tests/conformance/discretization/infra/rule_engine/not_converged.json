{
  "id": "not_converged",
  "description": "Rule $a -> +($a, 0) is non-terminating: every pass rewrites the root, the next pass walks the new root and fires again. At max_passes=3, the engine MUST abort with E_RULES_NOT_CONVERGED.",
  "rules": [
    {
      "name": "expand_add_zero",
      "pattern": "$a",
      "replacement": {"op": "+", "args": ["$a", 0]}
    }
  ],
  "input": "x",
  "max_passes": 3,
  "expect": {
    "kind": "error",
    "code": "E_RULES_NOT_CONVERGED"
  },
  "tags": ["not_converged", "max_passes"]
}
