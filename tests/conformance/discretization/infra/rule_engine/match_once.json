{
  "id": "match_once",
  "description": "Pattern +($a, 0) -> $a matches the single outer node and fires once. RFC §5.2.5 first-pass fires once, second-pass no-match, converges.",
  "rules": [
    {
      "name": "add_zero_int",
      "pattern": {"op": "+", "args": ["$a", 0]},
      "replacement": "$a"
    }
  ],
  "input": {"op": "+", "args": ["x", 0]},
  "expect": {
    "kind": "output",
    "canonical_json": "\"x\""
  },
  "tags": ["match_once"]
}
