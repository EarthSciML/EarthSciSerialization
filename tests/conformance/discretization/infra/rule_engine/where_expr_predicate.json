{
  "id": "where_expr_predicate",
  "description": "where-expression predicate (RFC §5.2.7). An ExpressionNode in the `where` slot is evaluated per query point; rule fires iff the predicate is truthy. Here `i == 0` is true at query_point i=0, so the rule fires.",
  "context": {
    "query_point": {"i": 0, "j": 3}
  },
  "rules": [
    {
      "name": "zero_at_west",
      "pattern":     {"op": "grad", "args": ["$u"], "dim": "$x"},
      "where":       {"op": "==", "args": ["i", 0]},
      "replacement": 0
    }
  ],
  "input": {"op": "grad", "args": ["T"], "dim": "x"},
  "expect": {
    "kind": "output",
    "canonical_json": "0"
  },
  "requires_per_point_scope": true,
  "tags": ["where_expr", "per_point"]
}
