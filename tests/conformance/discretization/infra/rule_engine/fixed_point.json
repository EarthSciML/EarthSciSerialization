{
  "id": "fixed_point",
  "description": "Pattern +($a, 0) -> $a requires two passes to fully reduce ((x + 0) + 0). Pass 1 seals the outer match after firing; pass 2 walks the new root and fires once more; pass 3 confirms stability.",
  "rules": [
    {
      "name": "add_zero_int",
      "pattern": {"op": "+", "args": ["$a", 0]},
      "replacement": "$a"
    }
  ],
  "input": {
    "op": "+",
    "args": [
      {"op": "+", "args": ["x", 0]},
      0
    ]
  },
  "expect": {
    "kind": "output",
    "canonical_json": "\"x\""
  },
  "tags": ["fixed_point"]
}
