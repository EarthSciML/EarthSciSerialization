{
  "category": "rule_engine",
  "version": "1.0",
  "description": "Cross-binding rule-engine fixtures per discretization RFC §5.2. Each fixture's `expect.canonical_json` is the byte-exact canonical form each binding's `canonical_json(rewrite(input, rules))` must produce. Error fixtures assert the engine aborts with the given stable error code.",
  "fixtures": [
    {"id": "match_once",         "path": "match_once.json",         "tags": ["match_once"]},
    {"id": "fixed_point",        "path": "fixed_point.json",        "tags": ["fixed_point"]},
    {"id": "not_converged",      "path": "not_converged.json",      "tags": ["not_converged", "max_passes"]},
    {"id": "nonlinear_match",    "path": "nonlinear_match.json",    "tags": ["nonlinear"]},
    {"id": "sibling_field",      "path": "sibling_field.json",      "tags": ["sibling_field"]},
    {"id": "guard_var_has_grid", "path": "guard_var_has_grid.json", "tags": ["guard", "var_has_grid"]},
    {"id": "region_index_range", "path": "region_index_range.json", "tags": ["region", "index_range"]},
    {"id": "region_index_range_fall_through", "path": "region_index_range_fall_through.json", "tags": ["region", "index_range", "fall_through"]},
    {"id": "region_boundary", "path": "region_boundary.json", "tags": ["region", "boundary"]},
    {"id": "region_mask_field", "path": "region_mask_field.json", "tags": ["region", "mask_field"]},
    {"id": "region_mask_field_fall_through", "path": "region_mask_field_fall_through.json", "tags": ["region", "mask_field", "fall_through"]},
    {"id": "where_expr_predicate", "path": "where_expr_predicate.json", "tags": ["where_expr", "per_point"]},
    {"id": "boundary_policy_per_axis", "path": "boundary_policy_per_axis.json", "tags": ["boundary_policy", "ghost_width", "per_axis"]},
    {"id": "ghost_width_uniform", "path": "ghost_width_uniform.json", "tags": ["boundary_policy", "ghost_width", "reflecting", "uniform"]},
    {"id": "guard_bind_side_axis", "path": "guard_bind_side_axis.json", "tags": ["guard", "bind_side_axis"]},
    {"id": "guard_bind_side_spacing", "path": "guard_bind_side_spacing.json", "tags": ["guard", "bind_side_spacing"]},
    {"id": "guard_bind_side_dim_size", "path": "guard_bind_side_dim_size.json", "tags": ["guard", "bind_side_dim_size"]},
    {"id": "mesh_boundary_bc", "path": "mesh_boundary_bc.json", "tags": ["region", "mask_field", "unstructured", "mpas", "bc"]},
    {"id": "bc_kind_side_match", "path": "bc_kind_side_match.json", "tags": ["guard", "bc", "kind_side"]},
    {"id": "bc_kind_discriminates", "path": "bc_kind_discriminates.json", "tags": ["guard", "bc", "kind_side"]},
    {"id": "bc_side_literal_discriminates", "path": "bc_side_literal_discriminates.json", "tags": ["guard", "bc", "kind_side"]},
    {"id": "bc_passthrough_preserves_kind", "path": "bc_passthrough_preserves_kind.json", "tags": ["guard", "bc", "kind_side", "canonical"]}
  ],
  "bindings_required": ["julia", "rust"],
  "bindings_optional": ["python", "go", "typescript"],
  "note": "Julia and Rust are the only bindings required to produce rule-engine output for Step 1 (RFC §13.1). Other bindings may round-trip the fixture's rule/input JSON without executing, or stub the rewrite step. Fixtures tagged with 'region' or 'where_expr' exercise RFC §5.2.7 per-query-point scoping; Julia and Rust must evaluate them against the fixture's context.query_point, while parse-only bindings MUST still load the fixture without error."
}
