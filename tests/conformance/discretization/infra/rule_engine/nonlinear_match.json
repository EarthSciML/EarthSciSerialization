{
  "id": "nonlinear_match",
  "description": "Non-linear pattern -($a, $a) -> 0 (§5.2.2). The two $a occurrences must bind to canonically-equal subtrees; -(x, x) rewrites to 0 and -(y, z) does not. Canonical form drops the int zero under +.",
  "rules": [
    {
      "name": "self_minus",
      "pattern": {"op": "-", "args": ["$a", "$a"]},
      "replacement": 0
    }
  ],
  "input": {
    "op": "+",
    "args": [
      {"op": "-", "args": ["x", "x"]},
      {"op": "-", "args": ["y", "z"]}
    ]
  },
  "expect": {
    "kind": "output",
    "canonical_json": "{\"args\":[\"y\",\"z\"],\"op\":\"-\"}"
  },
  "tags": ["nonlinear"]
}
