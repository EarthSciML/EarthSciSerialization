{
  "id": "ghost_width_uniform",
  "description": "Rule with the scalar (axis-uniform) ghost_width form and a string-form boundary_policy that uses one of the new (esm-bet) closed-set kinds. The rule engine itself does not branch on either field; the fixture confirms every binding parses both fields without error and rewrites the rule normally.",
  "rules": [
    {
      "name": "lf_passthrough",
      "pattern": {"op": "flux_advect", "args": ["$q"], "dim": "$x"},
      "boundary_policy": "reflecting",
      "ghost_width": 1,
      "replacement": "$q"
    }
  ],
  "input": {"op": "flux_advect", "args": ["q"], "dim": "x"},
  "expect": {
    "kind": "output",
    "canonical_json": "\"q\""
  },
  "tags": ["boundary_policy", "ghost_width", "reflecting", "uniform"]
}
