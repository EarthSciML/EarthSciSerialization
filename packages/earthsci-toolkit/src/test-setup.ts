/**
 * Test Setup for Core Library Tests
 *
 * Simple test setup for the core earthsci-toolkit library without UI dependencies.
 */

// No external testing library dependencies needed for core library tests